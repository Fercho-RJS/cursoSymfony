{% extends 'layout.html.twig' %}

{% block title %}Portada - Symfony 2025{% endblock %}

{% block contenido %}
	{# <div class="flash">
		{% for mensaje in app.flashes('info') %}
			<div class="alert alert-info">{{ mensaje }}</div>
		{% endfor %}
	</div> #}

	<div style="display: flex;">
		<div class="grid_6 alpha">
			<ul>
				{% for evento in eventosCol1 %}
					<li>
						<a href="{{ path('app_evento', {'slug': evento.slug} ) }}">{{ evento.titulo }}</a>
						<p>{{ evento.descripcion }}</p>
					</li>
				{% else %}
					<li>No hay ningún evento</li>
				{% endfor %}
			</ul>
		</div>

		<div class="grid_6 omega">
			<ul>
				{% for evento in eventosCol2 %}
					<li>
						<a href="{{ path('app_evento', {'slug': evento.slug} ) }} ">{{ evento.titulo }}</a>
						<p>{{ evento.descripcion }}</p>
					</li>
				{% else %}
					<li>No hay ningún evento</li>
				{% endfor %}
			</ul>
		</div>
	</div>
{% endblock %}
