<html>
	<!--  Borrar luego  -->
	<body>
		<h1>{{ evento.titulo }}</h1>
		<p>{{ evento.descripcion }}</p>
		<div class='grid-2-alpha'>
      <strong>Fecha</strong> {{ evento.fecha|date('d M') }}
    </div>
    <div class='grid-2'>
      <strong>Hora</strong> {{ evento.hora|date('H:i') }}
    </div>
    <div class='grid-2'>
      <strong>Idioma</strong> {{ evento.idioma }}
    </div>
    <div class='grid-3'>
      <strong>Disertante</strong> <a href="#R_A_D">{{ evento.disertante }}</a> 
    </div>
	</body>
</html><!--  Borrar luego  -->
